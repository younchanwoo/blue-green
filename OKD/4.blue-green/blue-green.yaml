# tomcat-service.yaml (프록시 역할)
apiVersion: v1
kind: Service
metadata:
  name: tomcat-service
  namespace: tomcat
spec:
  selector:
    app: tomcat
    version: blue
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
